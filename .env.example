# SpecFlux Frontend Environment Variables
#
# Copy this file to .env and fill in the values.
# For Vite, all client-side variables MUST be prefixed with VITE_
#
# For staging configuration, see .env.staging.example

# =============================================================================
# Backend API Configuration
# =============================================================================

# Spring Boot backend URL (local development)
# Note: Don't include /api - the generated client paths already include it
VITE_API_BASE_URL=http://localhost:8090

# =============================================================================
# Firebase Configuration (Local Development)
# =============================================================================

# For local development with Firebase Emulator, use these demo values:
VITE_FIREBASE_API_KEY=fake-api-key
VITE_FIREBASE_AUTH_DOMAIN=demo-specflux.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=demo-specflux

# Firebase Auth Emulator URL (local development only)
VITE_FIREBASE_AUTH_EMULATOR_URL=http://127.0.0.1:9099

# =============================================================================
# Environment Identification
# =============================================================================

# Environment name for display (local, staging)
# Leave empty or 'local' for local development
VITE_ENV_NAME=local

# =============================================================================
# Staging Configuration (Optional - for runtime switching)
# =============================================================================

# These are used when switching to staging via the UI environment switcher.
# The UI switcher allows developers to test against staging without restarting.
# For dedicated staging mode, use: npm run dev:staging

# Staging backend URL (Cloud Run)
# VITE_STAGING_API_BASE_URL=https://specflux-backend-xxxxx.us-west1.run.app

# Staging Firebase (production Firebase project)
# VITE_STAGING_FIREBASE_API_KEY=your-staging-firebase-api-key
# VITE_STAGING_FIREBASE_AUTH_DOMAIN=your-staging-project.firebaseapp.com
# VITE_STAGING_FIREBASE_PROJECT_ID=your-staging-project-id
