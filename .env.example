# SpecFlux Frontend Environment Variables
#
# Copy this file to .env and fill in the values.
# For Vite, all client-side variables MUST be prefixed with VITE_

# =============================================================================
# Backend API Configuration
# =============================================================================

# Spring Boot backend URL
# Note: Don't include /api - the generated client paths already include it
VITE_API_BASE_URL=http://localhost:8090

# =============================================================================
# Firebase Configuration
# =============================================================================

# For local development with Firebase Emulator, use these demo values:
VITE_FIREBASE_API_KEY=fake-api-key
VITE_FIREBASE_AUTH_DOMAIN=demo-specflux.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=demo-specflux

# Firebase Auth Emulator (leave empty for production Firebase)
VITE_FIREBASE_AUTH_EMULATOR_URL=http://127.0.0.1:9099

# =============================================================================
# Production Firebase Configuration (replace demo values above)
# =============================================================================

# Get these from Firebase Console > Project Settings > General > Your apps
# VITE_FIREBASE_API_KEY=your-actual-api-key
# VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id
# VITE_FIREBASE_AUTH_EMULATOR_URL=  # Leave empty for production
