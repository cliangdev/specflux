# SpecFlux Frontend Environment Variables (Staging)
#
# Copy this file to .env.staging and fill in the values.
# This file is for connecting to the staging backend (Cloud Run + Production Firebase).
#
# Usage:
#   npm run dev:staging     - Run with staging backend
#   npm run tauri:dev:staging - Run Tauri app with staging backend

# =============================================================================
# Backend API Configuration
# =============================================================================

# Cloud Run staging backend URL
# Get this from: gcloud run services describe specflux-backend --region=us-west1 --format='value(status.url)'
VITE_API_BASE_URL=https://specflux-backend-400514527718.us-west1.run.app

# =============================================================================
# Firebase Configuration (Production)
# =============================================================================

# Get these from Firebase Console > Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id

# Leave empty for staging - use production Firebase (no emulator)
VITE_FIREBASE_AUTH_EMULATOR_URL=

# =============================================================================
# Environment Identification
# =============================================================================

# Environment name for display purposes
VITE_ENV_NAME=staging
